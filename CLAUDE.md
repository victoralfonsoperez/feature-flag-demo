# Claude Code Guidelines

## Package management

- **Never install packages globally.** Use `npx <tool>`, `pnpm dlx <tool>`, or `corepack` instead.
- This project uses **pnpm**. Do not use `npm install` or `yarn`.
